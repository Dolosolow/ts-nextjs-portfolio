import styled from "styled-components";

import { StyledProps } from "@/styles/styled";

export const SlideLink = styled.div<StyledProps>`
  margin-right: auto;
  height: 100%;
  width: max-content;
  color: #fafafa;
  cursor: pointer;
`;
